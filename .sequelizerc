const path = require('path');

module.exports = {
  config: path.resolve('config', 'config.json'), // Caminho para o arquivo de configuração do Sequelize
  'models-path': path.resolve('models'), // Caminho para o diretório de modelos
  'seeders-path': path.resolve('seeders'), // Caminho para o diretório de seeders (opcional)
  'migrations-path': path.resolve('migrations') // Caminho para o diretório de migrações
};
